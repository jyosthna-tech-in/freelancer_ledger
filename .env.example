# Local development placeholders only. Copy to .env and keep secrets out of git.
# Do not put real AWS credentials, Cognito tokens, or personal data here.

API_BASE_URL=http://localhost:3000
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=test
AWS_SECRET_ACCESS_KEY=test
AWS_ENDPOINT_URL=http://localhost:4566

DYNAMODB_TABLE_NAME=freelancer-ledger-local
S3_BUCKET_NAME=freelancer-ledger-local

DEMO_USER_ID=local-demo-user
